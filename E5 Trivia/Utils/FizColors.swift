import SwiftUI

// MARK: - Fiz Color Palette
extension Color {
    // MARK: - Primary Colors

    /// Background Color - Warm off-white background (#f3eddf)
    static let fizBackground = Color(hex: "#f3eddf")

    /// Fox Orange - Primary brand orange (#dd7423)
    static let fizOrange = Color(hex: "#dd7423")

    /// Fox Cream - Light cream accent (#f4d29b)
    static let fizCream = Color(hex: "#f4d29b")

    /// Fox Brown (Shadow) - Deep brown for shadows and depth (#533214)
    static let fizBrown = Color(hex: "#533214")

    /// Teal Accent - Primary teal for streaks and highlights (#39766d)
    static let fizTeal = Color(hex: "#39766d")

    /// Teal Shadow - Darker teal for depth (#2e5147)
    static let fizTealShadow = Color(hex: "#2e5147")

    // MARK: - Secondary Colors

    /// Secondary Gray - Silver/gray accent (#b1aea5)
    static let fizGray = Color(hex: "#b1aea5")

    /// Dark Gold Star - Bronze/gold accent (#d48c20)
    static let fizDarkGold = Color(hex: "#d48c20")

    /// Light Gold Star - Bright gold highlight (#f3d29d)
    static let fizLightGold = Color(hex: "#f3d29d")

    /// Secondary Background - Slightly darker warm background (#e8dcc8)
    static let fizBackgroundSecondary = Color(hex: "#e8dcc8")
}
